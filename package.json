{
	"name": "auto-closing-tag-in-textarea",
	"version": "1.0.1",
	"description": "A library that supports the autoclosing tag function in textarea tag on the web",
	"main": "./dist/index.ts",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Ubinquitous",
	"license": "MIT",
	"dependencies": {
		"@types/react": "^18.0.28",
		"react": "^18.2.0"
	}
}
